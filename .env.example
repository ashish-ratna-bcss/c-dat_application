# Copy to .env and configure for your environment (not loaded automatically by PHP;
# use db_config.php or export these before starting PHP-FPM).

CDR_DB_HOST=127.0.0.1
CDR_DB_PORT=5432
CDR_DB_NAME=postgres
CDR_DB_USER=postgres
CDR_DB_PASSWORD=your_password_here
